[pytest]
testpaths = tests
markers =
    requires_boss: integration test that runs a real BOSS binary end-to-end (needs $BOSSdir set to a BOSS install with scripts/xZCM1A -- e.g. run inside the ligpargen:dev Docker image built from this repo's Dockerfile). See tests/fixtures/ and tests/test_integration_converters.py.
